// mockData.ts — no longer used. Supabase is the data source.
// Kept as an empty stub so any stale import doesn't break the build.
export const mockUsers = [];
export const mockMaterials = [];
export const mockReviews = [];
export const mockReports = [];
export const CURRENT_USER_ID = "";
export const ADMIN_EMAIL = "sameeropbis@gmail.com";
